apiVersion: v1
kind: Namespace
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: rancher-operator
    control-plane: controller-manager
  name: rancher-operator-system
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: clusterprerequisites.core.rancher-operator.io
spec:
  group: core.rancher-operator.io
  names:
    kind: ClusterPrerequisites
    listKind: ClusterPrerequisitesList
    plural: clusterprerequisites
    singular: clusterprerequisites
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetClusterRef.name
      name: Target
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterPrerequisites is the Schema for the clusterprerequisites
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClusterPrerequisites
            properties:
              applicationCollectionCredentialsSecretRef:
                description: |-
                  applicationCollectionCredentialsSecretRef references a Secret in the same
                  namespace carrying SUSE Application Collection credentials. The Secret
                  must contain "username" and "password" keys. These credentials are used
                  to (a) pull Helm charts over OCI from dp.apps.rancher.io and (b) provision
                  an image-pull Secret in each component's namespace on the target cluster
                  so chart pods can pull their container images. Required for any component
                  that is actually installed (i.e. not skipped, not bundled).
                properties:
                  name:
                    description: name of the referenced object.
                    type: string
                required:
                - name
                type: object
              certManager:
                description: |-
                  certManager configures the cert-manager installation. If omitted, cert-manager
                  is not managed by this resource and is reported as skipped.
                properties:
                  enabled:
                    default: true
                    description: enabled controls whether cert-manager is installed.
                    type: boolean
                  version:
                    description: |-
                      version pins the cert-manager Helm chart version. Defaults to a value
                      chosen by the operator if unset.
                    type: string
                required:
                - enabled
                type: object
              ingress:
                description: |-
                  ingress configures the ingress controller installation. If omitted, no ingress
                  controller is managed and the component is reported as skipped.
                properties:
                  enabled:
                    default: true
                    description: enabled controls whether the ingress controller is
                      installed.
                    type: boolean
                  type:
                    default: traefik
                    description: |-
                      type selects which ingress controller to install. Currently only "traefik"
                      is supported.
                    enum:
                    - traefik
                    type: string
                  useBundled:
                    default: auto
                    description: |-
                      useBundled controls whether the operator installs Traefik or trusts the
                      target cluster's bundled copy. "auto" (default) inspects the cluster's
                      Kubernetes version and skips install when it carries the "+k3s" or
                      "+rke2" suffix; "use" always skips install; "install" always runs Helm.
                    enum:
                    - auto
                    - use
                    - install
                    type: string
                  version:
                    description: |-
                      version pins the ingress controller Helm chart version. Defaults to a value
                      chosen by the operator if unset.
                    type: string
                required:
                - enabled
                type: object
              loadBalancer:
                description: |-
                  loadBalancer configures the load-balancer strategy. If omitted, the load
                  balancer is reported as skipped.
                properties:
                  metallb:
                    description: metallb carries MetalLB-specific configuration. Required
                      when type is "metallb".
                    properties:
                      addressPool:
                        description: |-
                          addressPool is the address range MetalLB will advertise via L2.
                          Accepts either CIDR notation ("192.168.1.0/24") or a hyphenated range
                          ("192.168.1.240-192.168.1.250").
                        type: string
                      version:
                        description: |-
                          version pins the MetalLB Helm chart version. Defaults to a value chosen
                          by the operator if unset.
                        type: string
                    required:
                    - addressPool
                    type: object
                  type:
                    description: type selects the load-balancer strategy.
                    enum:
                    - cloud
                    - metallb
                    - none
                    type: string
                required:
                - type
                type: object
              targetClusterRef:
                description: |-
                  targetClusterRef points to a TargetCluster in the same namespace where the
                  prerequisite components will be installed.
                properties:
                  name:
                    description: name of the referenced object.
                    type: string
                required:
                - name
                type: object
            required:
            - targetClusterRef
            type: object
          status:
            description: status defines the observed state of ClusterPrerequisites
            properties:
              components:
                description: components reports per-component install state.
                properties:
                  certManager:
                    description: ComponentStatus reports the install state of a single
                      prerequisite component.
                    properties:
                      installedVersion:
                        description: installedVersion is the chart/version actually
                          deployed on the target cluster.
                        type: string
                      message:
                        description: message carries a human-readable description
                          of the component's current state.
                        type: string
                      ready:
                        description: ready is true when the component is installed
                          and healthy on the target cluster.
                        type: boolean
                      skipped:
                        description: |-
                          skipped is true when the component was disabled in spec or detected as
                          pre-existing and therefore not managed by this resource.
                        type: boolean
                    type: object
                  ingress:
                    description: ComponentStatus reports the install state of a single
                      prerequisite component.
                    properties:
                      installedVersion:
                        description: installedVersion is the chart/version actually
                          deployed on the target cluster.
                        type: string
                      message:
                        description: message carries a human-readable description
                          of the component's current state.
                        type: string
                      ready:
                        description: ready is true when the component is installed
                          and healthy on the target cluster.
                        type: boolean
                      skipped:
                        description: |-
                          skipped is true when the component was disabled in spec or detected as
                          pre-existing and therefore not managed by this resource.
                        type: boolean
                    type: object
                  loadBalancer:
                    description: ComponentStatus reports the install state of a single
                      prerequisite component.
                    properties:
                      installedVersion:
                        description: installedVersion is the chart/version actually
                          deployed on the target cluster.
                        type: string
                      message:
                        description: message carries a human-readable description
                          of the component's current state.
                        type: string
                      ready:
                        description: ready is true when the component is installed
                          and healthy on the target cluster.
                        type: boolean
                      skipped:
                        description: |-
                          skipped is true when the component was disabled in spec or detected as
                          pre-existing and therefore not managed by this resource.
                        type: boolean
                    type: object
                type: object
              conditions:
                description: |-
                  conditions represent the current state of the ClusterPrerequisites resource.
                  Standard condition types: "Available", "Progressing", "Degraded".
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the .metadata.generation the status
                  was last reconciled against.
                format: int64
                type: integer
              ready:
                description: ready is true when all non-skipped components are installed
                  and healthy.
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: ranchermanagers.core.rancher-operator.io
spec:
  group: core.rancher-operator.io
  names:
    kind: RancherManager
    listKind: RancherManagerList
    plural: ranchermanagers
    singular: ranchermanager
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetClusterRef.name
      name: Target
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.installedVersion
      name: Version
      type: string
    - jsonPath: .status.url
      name: URL
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RancherManager is the Schema for the ranchermanagers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of RancherManager
            properties:
              bootstrapPasswordSecretRef:
                description: |-
                  bootstrapPasswordSecretRef references a Secret in the same namespace
                  holding the initial admin bootstrap password.
                properties:
                  key:
                    default: password
                    description: key within the Secret holding the password bytes.
                    type: string
                  name:
                    description: name is the Secret name in the same namespace as
                      the RancherManager.
                    type: string
                required:
                - name
                type: object
              hostname:
                description: hostname is the DNS name at which Rancher Manager will
                  be reachable.
                type: string
              ingressClass:
                default: traefik
                description: |-
                  ingressClass attached to Rancher's Ingress. Defaults to "traefik" to match
                  the ClusterPrerequisites default.
                type: string
              prerequisitesRef:
                description: |-
                  prerequisitesRef optionally references a ClusterPrerequisites resource
                  that must be Ready before installing or upgrading Rancher Manager.
                properties:
                  name:
                    description: name of the referenced object.
                    type: string
                required:
                - name
                type: object
              replicas:
                default: 3
                description: replicas controls how many Rancher Manager pods to run.
                format: int32
                minimum: 1
                type: integer
              targetClusterRef:
                description: |-
                  targetClusterRef points to a TargetCluster in the same namespace where
                  Rancher Manager will be installed.
                properties:
                  name:
                    description: name of the referenced object.
                    type: string
                required:
                - name
                type: object
              tls:
                description: tls configures the TLS source for the Rancher ingress.
                properties:
                  letsEncrypt:
                    description: letsEncrypt configures ACME issuance. Required when
                      source is "letsEncrypt".
                    properties:
                      email:
                        description: email registered with the ACME provider.
                        type: string
                      environment:
                        default: production
                        description: environment selects the production or staging
                          ACME endpoint.
                        enum:
                        - production
                        - staging
                        type: string
                    required:
                    - email
                    type: object
                  secret:
                    description: |-
                      secret references a kubernetes.io/tls Secret in the same namespace.
                      Required when source is "secret".
                    properties:
                      name:
                        description: name of the referenced object.
                        type: string
                    required:
                    - name
                    type: object
                  source:
                    description: source selects the TLS strategy.
                    enum:
                    - rancher
                    - letsEncrypt
                    - secret
                    type: string
                required:
                - source
                type: object
              version:
                description: |-
                  version is the Rancher Manager Helm chart version to deploy, e.g. "2.10.1".
                  Edit this field to trigger an upgrade.
                type: string
            required:
            - bootstrapPasswordSecretRef
            - hostname
            - targetClusterRef
            - tls
            - version
            type: object
          status:
            description: status defines the observed state of RancherManager
            properties:
              availableUpgrades:
                description: |-
                  availableUpgrades lists chart versions newer than installedVersion that the
                  operator has observed in the upstream Rancher Helm repository.
                items:
                  type: string
                type: array
              conditions:
                description: |-
                  conditions represent the current state of the RancherManager resource.
                  Standard condition types: "Available", "Progressing", "Degraded".
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              installedVersion:
                description: installedVersion is the chart version currently deployed
                  on the target cluster.
                type: string
              observedGeneration:
                description: observedGeneration is the .metadata.generation the status
                  was last reconciled against.
                format: int64
                type: integer
              phase:
                description: phase summarizes the install lifecycle.
                enum:
                - Pending
                - Installing
                - Available
                - Upgrading
                - Degraded
                type: string
              url:
                description: url is the full URL of the Rancher Manager UI/API once
                  available.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: targetclusters.core.rancher-operator.io
spec:
  group: core.rancher-operator.io
  names:
    kind: TargetCluster
    listKind: TargetClusterList
    plural: targetclusters
    singular: targetcluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.kubernetesVersion
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TargetCluster is the Schema for the targetclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TargetCluster
            properties:
              byo:
                description: byo configures a Bring-Your-Own target cluster. Must
                  be set when type is "byo".
                properties:
                  kubeconfigSecretRef:
                    description: |-
                      kubeconfigSecretRef points to a Secret in the same namespace as the
                      TargetCluster containing a kubeconfig under .data[key].
                    properties:
                      key:
                        default: kubeconfig
                        description: key is the data key within the Secret that holds
                          the kubeconfig bytes.
                        type: string
                      name:
                        description: name is the Secret name in the same namespace
                          as the TargetCluster.
                        type: string
                    required:
                    - name
                    type: object
                required:
                - kubeconfigSecretRef
                type: object
              type:
                description: type selects the provisioning mode for the target cluster.
                enum:
                - byo
                type: string
            required:
            - type
            type: object
          status:
            description: status defines the observed state of TargetCluster
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the TargetCluster resource.
                  Standard condition types: "Available", "Progressing", "Degraded".
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              kubernetesVersion:
                description: kubernetesVersion is the server version reported by the
                  target cluster.
                type: string
              nodeCount:
                description: nodeCount is the number of Nodes observed on the target
                  cluster.
                format: int32
                type: integer
              observedGeneration:
                description: observedGeneration is the .metadata.generation the status
                  was last reconciled against.
                format: int64
                type: integer
              ready:
                description: |-
                  ready reports whether the operator successfully reached the target cluster
                  on its last reconcile.
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: rancher-operator
  name: rancher-operator-controller-manager
  namespace: rancher-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: rancher-operator
  name: rancher-operator-leader-election-role
  namespace: rancher-operator-system
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: rancher-operator
  name: rancher-operator-clusterprerequisites-admin-role
rules:
- apiGroups:
  - core.rancher-operator.io
  resources:
  - clusterprerequisites
  verbs:
  - '*'
- apiGroups:
  - core.rancher-operator.io
  resources:
  - clusterprerequisites/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: rancher-operator
  name: rancher-operator-clusterprerequisites-editor-role
rules:
- apiGroups:
  - core.rancher-operator.io
  resources:
  - clusterprerequisites
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - core.rancher-operator.io
  resources:
  - clusterprerequisites/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: rancher-operator
  name: rancher-operator-clusterprerequisites-viewer-role
rules:
- apiGroups:
  - core.rancher-operator.io
  resources:
  - clusterprerequisites
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - core.rancher-operator.io
  resources:
  - clusterprerequisites/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: rancher-operator-manager-role
rules:
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - core.rancher-operator.io
  resources:
  - clusterprerequisites
  - ranchermanagers
  - targetclusters
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - core.rancher-operator.io
  resources:
  - clusterprerequisites/finalizers
  - ranchermanagers/finalizers
  - targetclusters/finalizers
  verbs:
  - update
- apiGroups:
  - core.rancher-operator.io
  resources:
  - clusterprerequisites/status
  - ranchermanagers/status
  - targetclusters/status
  verbs:
  - get
  - patch
  - update
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: rancher-operator-metrics-auth-role
rules:
- apiGroups:
  - authentication.k8s.io
  resources:
  - tokenreviews
  verbs:
  - create
- apiGroups:
  - authorization.k8s.io
  resources:
  - subjectaccessreviews
  verbs:
  - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: rancher-operator-metrics-reader
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: rancher-operator
  name: rancher-operator-ranchermanager-admin-role
rules:
- apiGroups:
  - core.rancher-operator.io
  resources:
  - ranchermanagers
  verbs:
  - '*'
- apiGroups:
  - core.rancher-operator.io
  resources:
  - ranchermanagers/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: rancher-operator
  name: rancher-operator-ranchermanager-editor-role
rules:
- apiGroups:
  - core.rancher-operator.io
  resources:
  - ranchermanagers
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - core.rancher-operator.io
  resources:
  - ranchermanagers/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: rancher-operator
  name: rancher-operator-ranchermanager-viewer-role
rules:
- apiGroups:
  - core.rancher-operator.io
  resources:
  - ranchermanagers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - core.rancher-operator.io
  resources:
  - ranchermanagers/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: rancher-operator
  name: rancher-operator-targetcluster-admin-role
rules:
- apiGroups:
  - core.rancher-operator.io
  resources:
  - targetclusters
  verbs:
  - '*'
- apiGroups:
  - core.rancher-operator.io
  resources:
  - targetclusters/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: rancher-operator
  name: rancher-operator-targetcluster-editor-role
rules:
- apiGroups:
  - core.rancher-operator.io
  resources:
  - targetclusters
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - core.rancher-operator.io
  resources:
  - targetclusters/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: rancher-operator
  name: rancher-operator-targetcluster-viewer-role
rules:
- apiGroups:
  - core.rancher-operator.io
  resources:
  - targetclusters
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - core.rancher-operator.io
  resources:
  - targetclusters/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: rancher-operator
  name: rancher-operator-leader-election-rolebinding
  namespace: rancher-operator-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: rancher-operator-leader-election-role
subjects:
- kind: ServiceAccount
  name: rancher-operator-controller-manager
  namespace: rancher-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: rancher-operator
  name: rancher-operator-manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: rancher-operator-manager-role
subjects:
- kind: ServiceAccount
  name: rancher-operator-controller-manager
  namespace: rancher-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: rancher-operator-metrics-auth-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: rancher-operator-metrics-auth-role
subjects:
- kind: ServiceAccount
  name: rancher-operator-controller-manager
  namespace: rancher-operator-system
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: rancher-operator
    control-plane: controller-manager
  name: rancher-operator-controller-manager-metrics-service
  namespace: rancher-operator-system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    app.kubernetes.io/name: rancher-operator
    control-plane: controller-manager
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: rancher-operator
    control-plane: controller-manager
  name: rancher-operator-controller-manager
  namespace: rancher-operator-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: rancher-operator
      control-plane: controller-manager
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        app.kubernetes.io/name: rancher-operator
        control-plane: controller-manager
    spec:
      containers:
      - args:
        - --metrics-bind-address=:8443
        - --leader-elect
        - --health-probe-bind-address=:8081
        command:
        - /manager
        image: controller:latest
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: manager
        ports:
        - containerPort: 8081
          name: health
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        volumeMounts: []
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: rancher-operator-controller-manager
      terminationGracePeriodSeconds: 10
      volumes: []
